{
  "name": "mealcrafter-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend --production=false && npm run build --prefix frontend && npm install --prefix backend --production=false && npx prisma generate --schema=backend/prisma/schema.prisma"
  }
}
