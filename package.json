{
  "name": "mealcrafter-monorepo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix frontend --production=false && npm run build --prefix frontend && npx prisma generate --schema=backend/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.0",
    "@prisma/client": "^7.9.0",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.22.0",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "prisma": "^7.9.0"
  }
}
